import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const ExportExcel: React.FC = () => {
    return (
        <div>
            <Title level={2}>Xuất file Excel</Title>
        </div>
    );
};

export default ExportExcel; 