import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const HealthEvents: React.FC = () => {
    return (
        <div>
            <Title level={2}>Báo cáo sự kiện y tế</Title>
        </div>
    );
};

export default HealthEvents; 