import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const MedicineReports: React.FC = () => {
    return (
        <div>
            <Title level={2}>Báo cáo số lượng thuốc</Title>
        </div>
    );
};

export default MedicineReports; 