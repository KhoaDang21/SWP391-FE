import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const VaccinationReports: React.FC = () => {
    return (
        <div>
            <Title level={2}>Báo cáo kiểm tra & tiêm chủng</Title>
        </div>
    );
};

export default VaccinationReports; 