import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const SendForm: React.FC = () => {
    return (
        <div>
            <Title level={2}>Gửi form xác nhận</Title>
        </div>
    );
};

export default SendForm; 