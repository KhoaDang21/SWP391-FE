import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const CreateForm: React.FC = () => {
    return (
        <div>
            <Title level={2}>Tạo form xác nhận</Title>
        </div>
    );
};

export default CreateForm; 