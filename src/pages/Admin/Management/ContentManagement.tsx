import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const ContentManagement: React.FC = () => {
    return (
        <div>
            <Title level={2}>Cập nhật tài liệu & bài viết</Title>
        </div>
    );
};

export default ContentManagement; 